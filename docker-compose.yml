version: '3.8'

# ── Database backend ─────────────────────────────────────────────────────────
# SQLite (default):  docker compose up
# PostgreSQL:        docker compose --profile postgres up
#
# Set DATABASE_URL in your .env file to match your choice.
# See .env.example for both options.
# ─────────────────────────────────────────────────────────────────────────────

services:
  intellibox:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: intellibox
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    ports:
      - "8000:8000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  # PostgreSQL database (activate with: docker compose --profile postgres up)
  postgres:
    image: postgres:16-alpine
    container_name: intellibox-db
    environment:
      - POSTGRES_DB=intellibox
      - POSTGRES_USER=intellibox
      - POSTGRES_PASSWORD=changeme
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
    profiles:
      - postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U intellibox -d intellibox"]
      interval: 5s
      timeout: 3s
      retries: 5

  # Testing profile: run the web app with test config
  intellibox-test:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: intellibox-test
    env_file:
      - .env.test
    environment:
      - DATABASE_URL=sqlite:///./data/intellibox.db
      - TESTING=true
    volumes:
      - ./data/test:/app/data
    ports:
      - "8001:8000"
    profiles:
      - testing
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 5s
      timeout: 3s
      start_period: 15s
      retries: 5

  # Test runner: runs the full test suite and exits
  test-runner:
    build:
      context: .
      dockerfile: Dockerfile.test
    container_name: intellibox-test-runner
    env_file:
      - .env.test
    profiles:
      - ci

volumes:
  postgres_data:
