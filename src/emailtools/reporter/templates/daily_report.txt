================================================================================
                        EMAILTOOLS DAILY REPORT
================================================================================

{{ generated_at.strftime('%A, %B %d, %Y at %I:%M %p') }}

SUMMARY
--------
Total Actions: {{ total_actions }}
  - High Priority:   {{ high_priority_count }}
  - Medium Priority: {{ medium_priority_count }}
  - Low Priority:    {{ low_priority_count }}

================================================================================
UNASSIGNED ACTIONS
================================================================================

{% if unassigned_actions -%}
{% for action in unassigned_actions %}
[{{ loop.index }}] {{ '[' + (action.priority or 'NONE').upper() + ']' }} {{ action.title }}
    From: {{ action.email.from_address }}
    Subject: {{ action.email.subject }}
    {% if action.due_date -%}
    Due Date: {{ action.due_date.strftime('%Y-%m-%d') }}
    {% endif -%}
    {% if action.confidence_score -%}
    Confidence: {{ "%.0f"|format(action.confidence_score * 100) }}%
    {% endif -%}
    {% if action.description -%}
    Description: {{ action.description[:150] }}{% if action.description|length > 150 %}...{% endif %}

    {% endif %}

{% endfor -%}
{% else -%}
No unassigned actions. Great work team!

{% endif %}
================================================================================
PROGRAM NEWS (LAST 7 DAYS)
================================================================================

{{ program_news }}

================================================================================

Generated by EmailTools | Emails processed today: {{ emails_today }}
Powered by Claude Code - https://github.com/anthropics/claude-code

================================================================================
